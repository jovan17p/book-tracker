controllers:
    resource:
        path: ../src/Controller/
        namespace: App\Controller
    type: attribute
    prefix: /{_locale}
    defaults:
        _locale: 'sr'
    requirements:
        _locale: en|sr|fr
